model Cliente {
  id              String @id @default(cuid())
  nombre          String
  correo          String
  telefono        String
  domicilio_envio String
  contrasena      String

  domicilio Domicilio_Cliente[]
  pedidos   Pedidos[]

  @@map("clientes")
}
